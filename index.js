export const getNumberOfIslands = ( grid ) => {

    // TODO find a L landmass, 
    // span out from there, save all connected coords
    // repeat, checking if next L is in previous landmass
    

    return totalIslands;
}